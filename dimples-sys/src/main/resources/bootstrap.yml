spring:
  application:
    name: dimples-sys
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        group: DEFAULT_GROUP
        prefix: dimples-sys
        file-extension: yaml
        server-addr: 139.9.220.155:8848
      discovery:
        server-addr: 139.9.220.155:8848
security:
  oauth2:
    resource:
      id: ${spring.application.name}
      user-info-uri: http://localhost:7002/auth/user